# 校园墙 · 用户功能清单

> 仅收录**用户可直接触发的操作**（有明确意图、产生状态变更）。纯查询（列表/详情/计数/日志/状态读取）归入末尾「只读查询」节。

---

## 1. 账号与认证

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 1.1 | 滑块验证授权 | `POST /api/slider-captcha/grant` | 5min TTL，IP 15次/min |
| 1.2 | 用户名查重 | `GET /api/user/check-username` | 注册时实时校验 |
| 1.3 | 昵称查重 | `GET /api/user/check-nickname` | 注册时实时校验 |
| 1.4 | 邀请码校验 | `GET /api/user/check-invite-code` | 可选填写 |
| 1.5 | 用户注册 | `POST /api/user/register` | 滑块验证码、UID 生成、邀请奖励 |
| 1.6 | 发送邮箱验证码 | `POST /api/user/send-email-code` | 邮箱唯一、频率限制 |
| 1.7 | 验证邮箱 | `POST /api/user/verify-email` | 验证码校验 |
| 1.8 | 账号密码登录 | `POST /api/user/login` | 滑块验证码、登录失败 10次/15min 限频 |
| 1.9 | 志学登录 | `POST /api/user/zhixue-login` | 滑块验证码、外部认证 |
| 1.10 | 志学唯一校验 | `POST /api/user/check-zhixue-unique` | 注册时校验 |
| 1.11 | 标记信任浏览器 | `POST /api/user/trust-browser` | 7天免登录 |
| 1.12 | 撤销信任浏览器 | `POST /api/user/revoke-trust` | 移除免登录 |
| 1.13 | 二维码登录-生成 | `GET /api/user/qrcode/generate` | 5min TTL |
| 1.14 | 二维码登录-确认授权 | `POST /api/user/qrcode/confirm` | 网页端扫码确认 |
| 1.15 | 忘记密码-发送验证码 | `POST /api/user/forgot-password/send-code` | 邮箱频率限制 |
| 1.16 | 忘记密码-重置密码 | `POST /api/user/forgot-password/reset` | 验证码校验 |
| 1.17 | 编辑个人资料 | `PATCH /api/user/me` | 昵称/头像/生日 |
| 1.18 | 信用点卡兑换 | `POST /api/user/redeem-credit` | Luhn 校验 |
| 1.19 | 信誉分兑换 | `POST /api/user/exchange-credibility` | 阶梯汇率（300→700→1000），季度上限 15 |
| 1.20 | PLUS 卡兑换 | `POST /api/user/redeem-plus-card` | Luhn 校验、激活订阅 |
| 1.21 | 志学绑定 | `POST /api/user/bind-zhixue` | 滑块验证码、实名 AES 加密 |
| 1.22 | 志学解绑 | `DELETE /api/user/bind-zhixue` | 清除认证 |
| 1.23 | 确认志学认证 | `POST /api/user/confirm-zhixue` | 用户确认 |
| 1.24 | 拒绝志学认证 | `POST /api/user/deny-zhixue` | 用户拒绝 |
| 1.25 | 管理员绑定 | `POST /api/user/bind-admin` | 关联管理员账号 |
| 1.26 | 管理员解绑 | `DELETE /api/user/bind-admin` | 取消关联 |
| 1.27 | 删除自己的帖子 | `DELETE /api/user/posts/:id` | 仅本人帖子 |
| 1.28 | 通知号申请 | `POST /api/notice-account/apply` | 滑块验证码、passkey 可选 |
| 1.29 | 申诉提交 | `POST /api/user/punishments/:id/appeal` | 每个惩罚限一次 |

---

## 2. 内容生产（帖子与讨论）

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 2.1 | 发帖 | `POST /api/posts` | 滑块验证码、信誉检查、内容 ≤50 字 |
| 2.2 | 点赞/取消 | `POST /api/posts/:id/like` | 互斥、SSE 广播 |
| 2.3 | 评论 | `POST /api/posts/:id/comments` | 信誉检查、敏感词 |
| 2.4 | 删除自己的评论 | `DELETE /api/posts/:postId/comments/:commentId` | 仅本人或帖子作者 |
| 2.5 | 创建讨论 | `POST /api/discussions` | 1min 限 5 次、最多 3 活跃 |
| 2.6 | 发布讨论评论 | `POST /api/discussions/:id/comments` | 信誉检查、敏感词 |
| 2.7 | 删除自己的讨论评论 | `DELETE /api/discussions/comments/:id` | 仅本人 |
| 2.8 | 举报帖子 | `POST /api/posts/:id/report` | 统一举报入口 |

### 配额

| 项目 | 免费 | 付费 | 重置 |
|------|------|------|------|
| 每日发帖 | 5/天 | 39 积分/条 | 每日 0:00 |
| 每日匿名帖 | 2/天 | 50 积分/条 | 每日 0:00 |
| 发帖频率 | 3/5min/用户 | 同 | 5min |

---

## 3. 问答（QA Bounty）

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 3.1 | 提问 | `POST /api/qa/questions` | 悬赏金额、截止时间、信誉检查 |
| 3.2 | 回答问题 | `POST /api/qa/questions/:id/answers` | 信誉检查、敏感词 |
| 3.3 | 删除自己的回答 | `DELETE /api/qa/answers/:id` | 仅本人 |
| 3.4 | 删除自己的问题 | `DELETE /api/qa/questions/:id` | 仅本人 |
| 3.5 | 采纳回答 | `POST /api/qa/questions/:id/accept/:aid` | 仅提问者 |
| 3.6 | 支付悬赏 | `POST /api/qa/questions/:id/reward` | 悬赏积分转账 |

### 配额

| 项目 | 免费 | 付费 | 重置 |
|------|------|------|------|
| 提问 | 3/周 | 100 积分/题 | 每周 |

---

## 4. 投票

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 4.1 | 用户发起投票 | `POST /api/notice/votes` | 需通知账号 |
| 4.2 | 参与投票 | `POST /api/votes/:id/vote` | IP/指纹去重、信誉检查 |
| 4.3 | 结束投票 | `POST /api/votes/:id/end` | 仅发起人 |
| 4.4 | 编辑投票 | `PUT /api/votes/:id` | 仅发起人、未开始 |

---

## 5. 失物拍卖

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 5.1 | 竞拍出价 | `POST /api/pickup/bid` | 匿名/实名选项 |
| 5.2 | 撤取出价 | `DELETE /api/pickup/my-bid/:bidId` | 仅本人 |
| 5.3 | 举报拍品 | `POST /api/pickup/report-content/:bidId` | 举报不当内容 |

---

## 6. 社交（悄悄话 / 主页留言 / 访问）

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 6.1 | 发送悄悄话 | `POST /api/whispers` | 信誉≥90、敏感词、每周限 2 |
| 6.2 | 签收悄悄话 | `POST /api/whispers/:id/sign` | 收件人确认 |
| 6.3 | 发送主页留言 | `POST /api/wall-messages` | 信誉≥70、60s 冷却、长度限 |
| 6.4 | 访问他人主页 | `POST /api/user/profile-visit` | 记录访客 |
| 6.5 | 解锁主页访问权限 | `POST /api/user/profile-visits/unlock` | 付费解锁 |

### 配额

| 项目 | 免费 | 付费 | 重置 |
|------|------|------|------|
| 悄悄话 | 2/周 | 20/周 | 周一 0:00 |
| 主页留言 | 2/天 | 59 积分/条 | 每日 |
| 留言长度 | 100 字符 | 200 字符 | — |
| 留言频率 | 60s/对 | 同 | — |

---

## 7. 任务中心与成长

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 7.1 | 签到 | `POST /api/user/checkin` | 每日 1 次，连签奖励 10–500 积分 |
| 7.2 | 补签 | `POST /api/user/checkin/repair` | 消耗积分 |
| 7.3 | 领取任务奖励 | `POST /api/user/daily-tasks/:id/claim` | 完成才可调 |
| 7.4 | 转盘旋转 | `POST /api/user/lucky-wheel/spin` | 5/天、需 ≥3 任务完成 |
| 7.5 | 触发成就检查 | `POST /api/user/achievements/check` | 行为触发解锁 |
| 7.6 | 完成新手任务 | `POST /api/user/newbie-task/complete` | 行为触发 |
| 7.7 | 领取阶段奖励 | `POST /api/user/newbie-task/claim-stage-reward` | 阶段 4 奖 3 天 PLUS++ |

### 配额

| 项目 | 免费 | 付费 | 重置 |
|------|------|------|------|
| 签到 | 1/天 | 同（奖励翻倍） | 每日 |
| 每日任务 | 3-4/天 | 5/天 | 每日 |
| 转盘 | 5/天 | 同 | 每日 |
| 新手任务 | 一次性 | — | 永不 |

---

## 8. 订阅与信用点

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 8.1 | 购买 PLUS++ | `POST /api/user/subscriptions` | weekly 399 / monthly 699 积分 |
| 8.2 | 续费 PLUS++ | `POST /api/user/subscriptions/renew` | 48h 内 15% 折扣 |
| 8.3 | 兑换信用点卡 | `POST /api/user/redeem-credit` | Luhn 校验 |
| 8.4 | 兑换 PLUS 卡 | `POST /api/user/redeem-plus-card` | Luhn 校验 |
| 8.5 | 信誉分→信用点兑换 | `POST /api/user/exchange-credibility` | 阶梯汇率、季度上限 15 |

### PLUS++ 特权对照

| 特权 | 免费 | PLUS++ |
|------|------|--------|
| 日发帖 | 5/天 | 无限 |
| 日匿名帖 | 2/天 | 50 积分/条 |
| 悄悄话 | 2/周 | 20/周 |
| 墙留言长度 | 100 字符 | 200 字符 |
| 墙留言 | 2/天 | 59 积分/条 |
| 昵称修改 | 199 积分 | 99 积分，1 次/月免费 |
| 帖子置顶 | — | 40/月免费，超 100 积分 |
| 签到奖励 | 10–500 | 翻倍 |
| 每日任务 | 3-4 | 5 |
| 帖子金框/头像 | 无 | 有 |

---

## 9. 通知与举报

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 9.1 | 发布通知 | `POST /api/notices` | 滑块验证码 |
| 9.2 | 编辑通知 | `PUT /api/notices/:id` | 仅本人 |
| 9.3 | 删除通知 | `DELETE /api/notices/:id` | 仅本人 |
| 9.4 | 置顶自己通知 | `POST /api/notices/:id/pin` | 仅本人 |
| 9.5 | 同步通知到讨论区 | `POST /api/notices/:id/sync` | 仅本人 |
| 9.6 | 统一举报 | `POST /api/reports` | 10 种内容类型 |
| 9.7 | 欺凌专项举报 | `POST /api/bullying-report` | 专项 |
| 9.8 | 提交反馈 | `POST /api/feedback` | 类型+描述+图片 |
| 9.9 | 标记通知已读 | `POST /api/user/notifications/mark-read` | 单条 |
| 9.10 | 标记全部已读 | `POST /api/user/notifications/mark-all-read` | 批量 |
| 9.11 | 标记留言已读 | `POST /api/wall-messages/mark-read` | 批量 |
| 9.12 | 标记访问记录已读 | `POST /api/user/profile-visits/mark-read` | 批量 |

---

## 10. 维护与系统（用户可触发）

| # | 功能 | 端点 | 关键规则 |
|---|------|------|----------|
| 10.1 | 维护模式-验证码绕过 | `POST /api/maintenance/verify` | test key 绕过 |
| 10.2 | 页面访问上报 | `POST /api/page-visit` | 统计 PV |

---

## 11. 只读查询（供参考，非用户操作）

> 以下均为**无状态变更**的读取接口，用户可查看但不"操作"。

| # | 功能 | 端点 |
|---|------|------|
| 11.1 | 帖子列表 | `GET /api/posts` |
| 11.2 | 帖子详情 | `GET /api/posts/:id` |
| 11.3 | 帖子图片 | `GET /api/posts/:id/image/:idx` |
| 11.4 | 评论列表 | `GET /api/posts/:id/comments` |
| 11.5 | 讨论列表 | `GET /api/discussions` |
| 11.6 | 讨论评论列表 | `GET /api/discussions/:id/comments` |
| 11.7 | QA 问题列表 | `GET /api/qa/questions` |
| 11.8 | QA 配额查询 | `GET /api/qa/quota` |
| 11.9 | QA 问题详情 | `GET /api/qa/questions/:id` |
| 11.10 | 我的问题 | `GET /api/qa/my-questions` |
| 11.11 | 投票列表/详情 | `GET /api/votes` `/api/votes/:id` |
| 11.12 | 拍卖列表/详情 | `GET /api/pickup/auctions` `/api/pickup/auction-detail/:slot` |
| 11.13 | 我的出价 | `GET /api/pickup/my-bids` |
| 11.14 | 今日拍品 | `GET /api/pickup/today-content` |
| 11.15 | 悄悄话收件箱 | `GET /api/whispers/inbox` |
| 11.16 | 主页留言收/发件箱 | `GET /api/wall-messages` `/api/wall-messages/sent` |
| 11.17 | 主页留言配额/未读 | `GET /api/wall-messages/quota` `/api/wall-messages/unread-count` |
| 11.18 | 主页访问记录 | `GET /api/user/profile-visits` |
| 11.19 | 主页访问权限检查 | `POST /api/user/profile-visits/check-access` |
| 11.20 | 通知列表 | `GET /api/notices` `/api/user/notices` |
| 11.21 | 公告 | `GET /api/announcement` |
| 11.22 | 通知未读数 | `GET /api/user/notifications/unread-count` |
| 11.23 | 个人通知列表 | `GET /api/user/notifications` |
| 11.24 | 通知号申请状态 | `GET /api/user/notice-app-notification` |
| 11.25 | 当前用户信息 | `GET /api/user/me` |
| 11.26 | 他人信息/帖子 | `GET /api/users/:id` `/api/users/:id/posts` |
| 11.27 | 搜索用户 | `GET /api/users/search` |
| 11.28 | 信用点日志 | `GET /api/user/credit-logs` |
| 11.29 | 信誉分信息/日志 | `GET /api/user/credibility-info` `/api/user/credibility-logs` |
| 11.30 | 签到日历 | `GET /api/user/checkin-calendar` |
| 11.31 | 每日任务列表 | `GET /api/user/daily-tasks` |
| 11.32 | 转盘可转次数 | `GET /api/user/lucky-wheel/can-spin` |
| 11.33 | 转盘历史 | `GET /api/user/lucky-wheel/history` |
| 11.34 | 成就列表 | `GET /api/user/achievements` |
| 11.35 | 新手任务进度 | `GET /api/user/newbie-task/progress` |
| 11.36 | 订阅状态/价格 | `GET /api/user/subscription/status` `/api/user/subscription/price` |
| 11.37 | 惩罚记录/详情 | `GET /api/user/punishments` `/api/user/punishments/:id` |
| 11.38 | 安全中心 | `GET /api/user/safety-center` |
| 11.39 | 我的举报 | `GET /api/user/my-reports` |
| 11.40 | 举报详情 | `GET /api/reports/:reportId` |
| 11.41 | 欺凌保护状态 | `GET /api/user/bullying-status` |
| 11.42 | 维护模式信息 | `GET /api/maintenance/info` |
| 11.43 | 版本信息 | `GET /api/version` |
| 11.44 | 全站统计 | `GET /api/stats` |
| 11.45 | 志学信息 | `GET /api/user/me/zhixue-info` |
| 11.46 | SSE 实时流 | `GET /api/stream` |

---

## 12. 后台/自动调用（非用户主动触发）

> 以下接口由前端自动发起，**无需用户主观意图**，但会产生状态变更。

| # | 功能 | 端点 | 触发时机 | 状态变更 |
|---|------|------|----------|----------|
| 12.1 | 心跳/在线 | `POST /api/user/heartbeat` | 定时器（在线状态） | 更新 `onlineUsers`、返回未读数 |
| 12.2 | 任务进度上报 | `POST /api/user/daily-tasks/progress` | 业务行为（发帖/评论/点赞等） | `currentCount++` |
| 12.3 | 过期检查 | `POST /api/user/subscriptions/check-expiry` | 登录/定时 | 标记过期订阅状态 |

---

## 13. 特殊功能说明

> 下列功能行为较复杂或易误解，列出供开发与排查参考。

### 13.1 滑块验证码（Slider Captcha）

- **授权与消费分离**：`grant` 获取 `captchaId`（5min TTL），业务请求携带 `captchaId + text`。
- **仅成功时消费**：业务操作**成功**后 `captchaStore.delete(captchaId)`；**失败时保留**，用户无需重新滑动即可重试。
- **bot 测试豁免**：`maintenance.isBotTesting()` 为 true 时不消费验证码（E2E 测试用）。
- **覆盖的业务**：注册、登录、志学登录、发帖、志学绑定、通知号申请。

### 13.2 帖子可见性（Visibility）

- **四种模式**：`public`（默认）/ `self_only`（仅自己）/ `whitelist`（白名单）/ `blacklist`（黑名单）。
- **创建后不可变**：`visibleTo` / `invisibleTo` 在发帖时确定，后续不可修改。
- **例外：`no_violation` 恢复**：管理员处理举报时若判定 `no_violation`，可将 `self_only` 恢复为 `public`（`routes/admin.js:517-518`）。
- **作者始终可见**：无论可见性如何，帖子作者本人始终能看到自己的帖子。
- **敏感词强制 `self_only`**：发帖时若命中敏感词且未声明 `sensitiveForce`，自动降级为 `self_only`（`routes/posts.js:544`）。

### 13.3 敏感词与欺凌名称

- **双库检测**：`sensitiveWords.js`（腾讯词库 + 自定义）+ `bullyingNames.js`（受保护名称）。
- **命中行为**：
  - 帖子/评论/悄悄话/留言：**拒绝提交**（返回 `SENSITIVE_WORDS`）。
  - 帖子 `sensitiveForce=true`：跳过检查但**仍生成自动举报**（`sensitive_post`）。
  - 欺凌名称命中：返回 `BULLYING_NAME`，内容被拒。
- **词库更新**：通过管理员 API 动态修改，无需重启。

### 13.4 通知双写模型

- **两张表**：`notices`（通知体）+ `user_notifications`（已读状态桥接）。
- **自动触发需双写**：写入 `notices` 后必须调用 `addUserNotification`，否则 badge 计数丢失。
- **标记已读只影响 badge**：`mark-read` 仅修改 `user_notifications.read`，**不会**让通知从列表消失。
- **未读计数**：`getUnreadCount` 查 `user_notifications WHERE read=0`。

### 13.5 T0/T1 惩罚叠加规则

| 场景 | 结果 |
|------|------|
| 新 T0 + 已有 T0 | 合并，取最长持续时间 |
| 新 T0 + 已有 T1 | T0 生效，已有 T1 标记 `queued`（T0 过期后自动激活） |
| 新 T1 + 已有 T0 | 新 T1 标记 `queued`，T0 过期后激活 |
| 新 T1 + 已有 T1 | 合并 measures（取并集），取最长持续时间 |

- **T0（全封禁）**：`isFeatureBlocked()` 对所有功能返回 true。
- **T1（部分封禁）**：仅 `measures` 中列定的功能被封（`whisper / anonymous_post / qa / post / vote / wall_message`）。
- **自动过期**：定时检查 `expiresAt`，到期自动标记 `expired` 并激活 `queued` 惩罚。
- **申诉**：每个惩罚限一次申诉，管理员 `approve` 后撤销惩罚 + 恢复信誉分。

### 13.6 信誉分阈值与功能封锁

| 信誉分 | 限制 |
|--------|------|
| < 90 | 禁止发送悄悄话 |
| < 85 | 禁止匿名发帖 |
| < 80 | 禁止 QA（提问/回答） |
| < 70 | 禁止发送主页留言 |
| < 60 | 禁止发帖 |
| < 50 | 禁止投票 |

- **初始值**：90（学生认证 +10）。
- **信用点兑换信誉分**：阶梯汇率（前 5 点 300 积分/点，次 5 点 700，最后 5 点 1000），每季度上限 15 分。
- **季度刷新**：1/3/6/9 月 1 日。

### 13.7 悄悄话（Whisper）

- **不能给自己发**：`receiverId === session.id` 时拒绝。
- **内容长度**：≤50 字符。
- **每周配额**：非 PLUS 2/周，PLUS 20/周；超出自动扣 200 积分/条。
- **签收机制**：收件人点击签收 → 发送者收到通知。
- **接收者不存在/被封禁**：拒绝发送。

### 13.8 主页留言（Wall Message）

- **信誉门槛**：≥70 才能发送。
- **频率限制**：每对发送者-接收者 60s 冷却（`wallMessageRateLimit`，内存状态，重启丢失）。
- **内容长度**：非 PLUS ≤100 字符，PLUS ≤200 字符。
- **每日配额**：2/天免费，超出 59 积分/条。

### 13.9 每日任务进度上报

- **自动触发**：业务操作（发帖/评论/点赞/悄悄话/访问/搜索/浏览/通知）完成后，业务路由内部调用 `updateTaskProgress`。
- **非用户主动**：用户无需手动上报，前端调用 `/api/user/daily-tasks/progress` 仅是内部进度同步。
- **任务池**：8 种基础任务 + 1 种 PLUS 专属任务（本周获赞）。
- **随机生成**：每日 0:00 后首次访问时随机抽取（非 PLUS 3-4 个，PLUS 5 个）。

### 13.10 二维码登录流程

1. 网页端 `GET /api/user/qrcode/generate` → 获取 QR Token（5min TTL）+ 滑块验证码。
2. 移动端扫码 `GET /api/user/qrcode/scan` → 携带 userToken 绑定到 QR Token。
3. 网页端轮询 `GET /api/user/qrcode/status` → 等待移动端绑定。
4. 网页端确认 `POST /api/user/qrcode/confirm` → 完成登录。

### 13.11 订阅卡兑换

- **Luhn 校验**：卡号通过 Luhn 算法校验（`lib/subscription.js:luhnModN`）。
- **一次性使用**：`status` 从 `unused` → `used`，绑定 `usedBy` + `usedAt`。
- **PLUS 卡**：直接激活对应时长的订阅（weekly/monthly）。
- **信用点卡**：直接增加用户信用点余额。

### 13.12 SSE 实时推送

- **事件类型**：`postUpdate` / `discussionUpdate` / `noticeUpdate` / `voteUpdate` / `pickupUpdate` / `qaUpdate` / `announcementUpdate` / `subscriptionUpdate` / `profileVisit` / `ping`。
- **前端监听**：`index.html` 仅主动注册 `postUpdate` 和 `noticeUpdate`；其他事件为下游消费者预留。
- **心跳**：服务端每 15s 发送 `ping`（`lib/sse.js`），保持连接。

---

## 前端页面地图

| 页面 | 路径 | 含用户操作 |
|------|------|------------|
| 主页 SPA | `index.html` | 发帖、点赞、搜索、悄悄话、讨论、举报 |
| 帖子详情 | `post.html` | 评论、点赞、举报 |
| 个人中心 | `user.html` | 资料编辑、志学绑定、主页留言、访客、订阅、安全中心、二维码登录 |
| 通知中心 | `notice.html` | 发布/编辑/删除/置顶/同步通知 |
| QA 问答 | `qa.html` | 提问、回答、采纳、悬赏 |
| 投票 | `vote.html` | 发起/参与/结束/编辑投票 |
| 失物拍卖 | `pickup.html` | 出价、撤除、举报 |
| PLUS 订阅 | `plus.html` | 购买、续费、PLUS 卡 |
| 信用点 | `credit.html` | 信用点卡兑换、日志 |
| 任务中心 | `task-center.html` | 签到、任务奖励、转盘、成就、新手任务 |
| 安全中心 | `safety.html` | 查看惩罚、提交申诉 |
| 举报中心 | `report.html` | 提交举报、查看我的举报 |
| 欺凌举报 | `bully.html` | 专项举报 |
| 知识库 | `knowledge.html` | 静态浏览 |
| 生态圈 | `ecosystem.html` | 导航 |
| 用户协议 | `agreement.html` | 静态 |
| 通知号申请 | `apply-notice.html` | 申请发布权限 |
| 精选内容 | `featured.html` | 静态浏览 |
| 启动页 | `launch.html` | 入口 |
| 介绍页 | `intro.html` | 静态（维护模式豁免） |

---

*生成时间：2026-08-07 · 用户可操作功能 92 项 + 只读查询 46 项 + 后台自动调用 3 项，覆盖全部 21 个后端模块 + 21 个前端页面。*
